// examples/demo.ltl — runnable demo for ltl_pbkdf2

import std::io
use ltl_pbkdf2 as lib

fn main() {
    io::println("ltl_pbkdf2 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}